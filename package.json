{
  "name": "claude-copilot-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action для интеграции Claude Code с GitHub Issues через систему лейблов",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "bun test"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.12",
    "@types/node": "^20.0.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  }
}
